Just experimental github action to automatically publish update module into nest.land
when we push new tag version.

This action should do following steps:

* install eggs cli
* linking key
* echoing tag (for now)